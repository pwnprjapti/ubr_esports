<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
    <title>Category | UBR Esports Admin</title>
    <link rel="icon" type="image/jpeg" href="/images/photo.jpg.jpeg">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" />
    <link rel="stylesheet" href="/css/admin/adminLayout.css">
    <link rel="stylesheet" href="/css/admin/category.css">
    <style>
        .delete-tournament-btn {
            position: absolute;
            top: 0.75rem;
            right: 0.75rem;
            background: rgba(215, 38, 61, 0.85);
            border: none;
            border-radius: 50% !important;
            width: 34px !important;
            height: 34px !important;
            padding: 0 !important;
            box-sizing: border-box !important;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #ffffff;
            cursor: pointer;
            transition: transform 0.2s ease, background-color 0.2s ease, box-shadow 0.2s ease;
            z-index: 10;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
        }

        .delete-tournament-btn:hover {
            transform: scale(1.1);
            background-color: #ff455e;
            box-shadow: 0 4px 12px rgba(215, 38, 61, 0.5);
        }

        /* Date Banner styling */
        .selected-date-banner {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 1rem;
            margin: 1.25rem auto;
            width: 100%;
            max-width: 600px;
        }

        .selected-date-banner .line {
            flex: 1;
            height: 1px;
            background: rgba(255, 69, 94, 0.4);
        }

        .selected-date-banner .date-text {
            color: #ff455e;
            font-size: 0.85rem;
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            background: transparent;
            border: none;
            padding: 0 0.5rem;
            text-shadow: none;
            white-space: nowrap;
        }
    </style>
</head>
<body>
    <div class="admin-layout">
        <%- include("../partials/sidebar") %>

        <div class="admin-main">
            <%- include("../partials/adminNav") %>

            <main class="admin-body">
                <div class="admin-category-page">
                    <!-- Hero Header -->
                    <section class="category-hero">
                        <div class="hero-text">
                            <!-- <div class="hero-badge" style="display: inline-flex; align-items: center; gap: 0.4rem; padding: 0.25rem 0.65rem; background: rgba(215,38,61,0.15); border: 1px solid rgba(215,38,61,0.3); border-radius: 999px; font-size: 0.75rem; font-weight: 700; color: #ff455e; margin-bottom: 0.5rem;">
                                <i class="fa-solid fa-circle" style="font-size: 0.45rem;"></i> Date-Wise Category Schedule
                            </div> -->
                        <div class="hero-text" style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem;">
                            <div>
                                <h1>BGMI Tournaments & Scrims</h1>
                                <p>Select a date to find live scrims, daily cash cups, and weekend custom matches.</p>
                            </div>
                            <a href="/admin/addtournament" class="slot-btn-full" style="width: auto; padding: 0.65rem 1.25rem; text-decoration: none;">
                                <i class="fa-solid fa-plus"></i> Add Tournament
                            </a>
                        </div>
                    </section>

                    <!-- Date-Based Filter Section -->
                    <section class="date-filter-section">
                        <div class="filter-header">
                            <div class="filter-title">
                                <i class="fa-solid fa-calendar-days"></i>
                                <span>Filter by Tournament Date</span>
                            </div>
                        </div>

                        <div class="filter-controls-wrapper">
                            <!-- Specific Custom Date Picker -->
                            <div class="custom-date-picker">
                                <label for="customDatePicker"><i class="fa-regular fa-calendar"></i> Pick Specific Date:</label>
                                <input type="date" id="customDatePicker" name="customDate" min="2026-08-01" max="2026-12-31">
                            </div>
                        </div>
                    </section>

                    <!-- Selected Date Display Banner -->
                    <div class="selected-date-banner">
                        <span class="line"></span>
                        <span class="date-text" id="selectedDateDisplay">Loading Date...</span>
                        <span class="line"></span>
                    </div>

                    <!-- Tournament / Category Grid -->
                    <section class="tournament-grid" id="tournamentGrid">

                        <!-- Card 1 -->
                          <% for (const match of matches) { %>
                        <article class="tournament-card" data-date="<%= match.date %>" style="position: relative;">
                             <button class="delete-tournament-btn" onclick="deleteTournament(event, this, '<%= id %>', '<%= match._id %>')" title="Delete Tournament">
                                 <i class="fa-solid fa-trash-can"></i>
                             </button>
                            <h3 class="tournament-title"><%= match.title %></h3>
                            <div class="tournament-meta">
                                <div class="meta-item">
                                    <span class="meta-label"><i class="fa-regular fa-calendar"></i> Match Date</span>
                                    <span class="meta-val highlight-date"><%= match.date %></span>
                                </div>
                                <div class="meta-item">
                                    <span class="meta-label"><i class="fa-solid fa-sack-dollar"></i> Prize Pool</span>
                                    <span class="meta-val prize">₹ <%= match.prizePool %></span>
                                </div>
                                <div class="meta-item" style="display: flex; flex-direction: column; align-items: flex-start; gap: 0.4rem; width: 100%;">
                                    <div style="display: flex; justify-content: space-between; width: 100%; align-items: center;">
                                        <span class="meta-label"><i class="fa-solid fa-users"></i> Slots Booked</span>
                                        <span class="meta-val slot-highlight"><%= (match.teams ? match.teams.length : 0) %> / <%= match.slots %></span>
                                    </div>
                                    <% const filledPercentage = Math.min(100, Math.round(((match.teams ? match.teams.length : 0) / (match.slots || 1)) * 100)); %>
                                    <div class="progress-bar-container" style="width: 100%; height: 8px; background: rgba(255, 255, 255, 0.08); border-radius: 999px; overflow: hidden; border: 1px solid rgba(255, 255, 255, 0.04); box-sizing: border-box;">
                                        <div class="progress-bar-fill" style="width: <%= filledPercentage %>%; height: 100%; background: linear-gradient(90deg, #ff455e, #d7263d); border-radius: 999px; transition: width 0.3s ease;"></div>
                                    </div>
                                </div>
                                <div class="meta-item">
                                    <span class="meta-label"><i class="fa-solid fa-rupee-sign"></i> Entry Fee</span>
                                    <span class="meta-val slot-highlight">₹<%= match.entryFee %></span>
                                </div>
                            </div>
                            <div class="idp-timing">
                                <span class="meta-label"><i class="fa-regular fa-clock"></i> IDP Timing</span>
                                <div class="timings">
                                    <% for( time of match.idpTimings.split(",") ) { %>
                                    <span><%= time %></span>
                                    <% } %>
                                </div>
                            </div>
                            <% if (match.maps) { %>
                            <div class="idp-timing" style="margin-top: 10px;">
                                <span class="meta-label"><i class="fa-solid fa-map"></i> Maps</span>
                                <div class="timings">
                                    <% for( mapName of match.maps.split(",") ) { %>
                                    <span><%= mapName.trim() %></span>
                                    <% } %>
                                </div>
                            </div>
                            <% } %>
                            <div class="card-actions-single">
                                <button class="slot-btn-full" onclick="window.location.href='/admin/teams/<%= id %>/<%= match._id %>'">
                                    <i class="fa-solid fa-eye"></i> View Details
                                </button>
                            </div>
                        </article>
                       <% } %>
                        
                    </section>

                    <!-- Empty State -->
                    <div class="no-tournaments-card" id="noTournamentsMsg" style="display: none;">
                        <div class="empty-icon"><i class="fa-regular fa-calendar-xmark"></i></div>
                        <h3>No Tournaments Found on this Date</h3>
                        <p>There are no matches scheduled for the selected date.</p>
                        <button type="button" class="reset-filter-btn" id="resetDateFilter">
                            <i class="fa-solid fa-rotate-left"></i> View All Dates
                        </button>
                    </div>
                </div>
            </main>

            <%- include("../partials/footer") %>
        </div>
    </div>

    <!-- Date Filter Script -->
    <script>
        (function() {
            const customDatePicker = document.getElementById('customDatePicker');
            const tournamentCards = document.querySelectorAll('.tournament-card');
            const emptyMsg = document.getElementById('noTournamentsMsg');
            const resetBtn = document.getElementById('resetDateFilter');

            function applyFilter(customDateValue) {
                let visibleCount = 0;
                tournamentCards.forEach(card => {
                    const cardDate = card.getAttribute('data-date');
                    if (!customDateValue || cardDate === customDateValue) {
                        card.style.display = 'flex';
                        visibleCount++;
                    } else {
                        card.style.display = 'none';
                    }
                });

                if (visibleCount === 0) {
                    emptyMsg.style.display = 'block';
                } else {
                    emptyMsg.style.display = 'none';
                }

                // Update selected date display banner
                const dateDisplay = document.getElementById('selectedDateDisplay');
                if (dateDisplay) {
                    if (customDateValue) {
                        try {
                            const dateParts = customDateValue.split('-');
                            const formattedDate = new Date(dateParts[0], dateParts[1] - 1, dateParts[2]);
                            const options = { weekday: 'long', day: 'numeric', month: 'long', year: 'numeric' };
                            dateDisplay.textContent = formattedDate.toLocaleDateString('en-US', options);
                        } catch(e) {
                            dateDisplay.textContent = customDateValue;
                        }
                    } else {
                        dateDisplay.textContent = "All Upcoming Matches";
                    }
                }
            }

            if (customDatePicker) {
                customDatePicker.addEventListener('change', function() {
                    applyFilter(this.value);
                });
            }

            if (resetBtn) {
                resetBtn.addEventListener('click', function() {
                    if (customDatePicker) customDatePicker.value = '';
                    applyFilter(null);
                });
            }

            // Pre-fill current date (today) in YYYY-MM-DD format and apply filter
            const today = new Date();
            const year = today.getFullYear();
            const month = String(today.getMonth() + 1).padStart(2, '0');
            const day = String(today.getDate()).padStart(2, '0');
            const todayString = `${year}-${month}-${day}`;

            if (customDatePicker) {
                customDatePicker.value = todayString;
                applyFilter(todayString);
            }
        })();
    </script>

    <script>
        async function deleteTournament(event, btn, categoryId, matchId) {
            event.stopPropagation();
            if (confirm("Are you sure you want to delete this tournament? This action cannot be undone.")) {
                try {
                    const originalBtnHtml = btn.innerHTML;
                    btn.disabled = true;
                    btn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i>';

                    const res = await fetch(`/admin/category/${categoryId}/tournament/delete/${matchId}`, {
                        method: 'POST'
                    });
                    const result = await res.json();
                    alert(result.msg);
                    if (res.status === 200 || result.success) {
                        window.location.reload();
                    } else {
                        btn.disabled = false;
                        btn.innerHTML = originalBtnHtml;
                    }
                } catch(err) {
                    console.error("Error deleting tournament:", err);
                    alert("Something went wrong. Please try again.");
                    if (typeof btn !== 'undefined' && originalBtnHtml) {
                        btn.disabled = false;
                        btn.innerHTML = originalBtnHtml;
                    }
                }
            }
        }
    </script>
</body>
</html>
